/*
 * ============================================================
 * image_loader.h (nodes_editor)
 * ============================================================
 * وحدة تحميل الصور المشتركة لمحرر التطوير نفسه - معاكس تماماً
 * لنظيرتها بـsrc/nodes/image_loader.h (تلك gsKit، هذي stb_image +
 * فك تشفير يدوي لصيغ PS2/PS1 الأصلية). أي عقدة محرر تحتاج معاينة
 * صورة (Sprite2D حالياً، وأي عقدة مستقبلية غيرها) تنادي نفس الدالة.
 *
 * الصيغ المدعومة: PNG/JPEG/BMP/TGA/TIFF (عبر stb_image الجاهزة)،
 * TIM2/TM2 وTIM (فك تشفير يدوي - نفس المرجعين الحقيقيين المستخدمين
 * بنسخة PS2: ps2_tim2_tool.py وps1_tim_tool.py)، RAW (صيغ الألوان
 * المباشرة بس - 32/24/16/16S بت؛ RAW المفهرسة 8/4-بت مو مدعومة هنا
 * لأن RAW أصلاً بلا أي قسم CLUT بالملف نفسه - لا مصدر ألوان نفكّها
 * منه. TIM2/TIM المفهرسة مدعومة بالكامل لأنها تحمل CLUT داخل الملف).
 * غير مدعومة إطلاقاً: GIF (فخ تسمية - راجع النقاش السابق).
 * ============================================================
 */

#ifndef IMAGE_LOADER_EDITOR_H
#define IMAGE_LOADER_EDITOR_H

/* يحمّل أي صيغة مدعومة ويرجّع بكسلات RGBA8888 خام (4 بايت للبكسل،
 * صف تلو صف من الأعلى) جاهزة مباشرة لـwindow_create_texture - مؤشر
 * مخصَّص بـmalloc، تحرير المتصل مسؤوليته (free عادي). NULL لو فشل
 * التحميل أو الصيغة غير مدعومة.
 *
 * raw_width/raw_height/raw_psm تُستخدم فقط لو كان امتداد الملف
 * ".raw" (نفس ثوابت GS_PSM_* المستخدمة بنسخة PS2 - GS_PSM_32=0،
 * GS_PSM_24=1، GS_PSM_16=2، GS_PSM_16S=0x0A - راجع تعليق الهيدر
 * فوق ليش المفهرسة 8/4-بت مو مدعومة لـRAW تحديداً) */
unsigned char *image_loader_editor_load(const char *path, int *out_width, int *out_height,
                                         int raw_width, int raw_height, int raw_psm);

#endif /* IMAGE_LOADER_EDITOR_H */
