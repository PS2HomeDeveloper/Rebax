/*
 * ============================================================
 * element.c (nodes_editor)
 * ============================================================
 * التمثيل البصري لـElement بمحرر التطوير - Element عقدة مجردة بلا
 * أي خاصية مكانية (بلا Position)، فما فيها شيء نرسمه هنا (draw_2d
 * وdraw_3d كلاهما غير مصرَّح، يبقيان NULL تلقائياً). موجود بهذا
 * الملف فقط لثبات البنية - كل نوع عقدة له ملف مقابل هنا، حتى لو
 * فاضي بصرياً حالياً.
 * ============================================================
 */

/* @NODE_EDITOR type=NODE_TYPE_ELEMENT */
