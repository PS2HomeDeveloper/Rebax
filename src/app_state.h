/*
 * ============================================================
 * app_state.h
 * ============================================================
 * يحدد أي "شاشة رئيسية" من شاشات البرنامج معروضة الآن. مكتبة
 * صغيرة مستقلة عشان أي ملف يحتاج يطلب انتقال لشاشة ثانية (مثلاً
 * project_dialog بعد نجاح الإنشاء) ما يحتاج يعرف أي شيء عن تفاصيل
 * تلك الشاشة (editor_workspace، إلخ) - بس يطلب التبديل بالاسم.
 * ============================================================
 */

#ifndef APP_STATE_H
#define APP_STATE_H

typedef enum {
    APP_SCREEN_PROJECT_CENTER, /* شاشة قائمة المشاريع (البداية) */
    APP_SCREEN_EDITOR          /* واجهة تطوير المشروع المفتوح حالياً */
} app_screen_t;

/* يبدّل الشاشة الحالية */
void app_state_set_screen(app_screen_t screen);

/* يرجع الشاشة الحالية */
app_screen_t app_state_get_screen(void);

#endif /* APP_STATE_H */
