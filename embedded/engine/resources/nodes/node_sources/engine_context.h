/*
 * ============================================================
 * engine_context.h
 * ============================================================
 * سياق التشغيل العام اللي أي عقدة PS2 تحتاج وصول له وقت init/
 * update/draw - حالياً فقط GSGLOBAL (سياق الرسوميات العام)، لأنه
 * أول شيء عقدة حقيقية (Sprite2D) احتاجته فعلياً. يُنفَّذ من كود
 * تشغيل اللعبة نفسه (main loop على PS2 - خطوة تصدير قادمة منفصلة،
 * غير مكتوبة بعد) - العقد هنا تستدعيه بس، ما تعرّفه.
 *
 * ليش دالة، مو متغير extern مباشر؟ عشان لا نلزم شكل تخزينه (متغير
 * عام، Singleton، ثابت بتابع تحكم...) - أي عقدة تحتاج GS تنادي
 * الدالة بس، وين ما كان مصدرها الحقيقي لاحقاً.
 * ============================================================
 */

#ifndef ENGINE_CONTEXT_H
#define ENGINE_CONTEXT_H

/* GSGLOBAL معرَّف بـgsKit.h (مكتبة gsKit، الجزء المُضمَّن ببيئة
 * PS2dev المندمجة مع الملف التنفيذي - راجع تعليق Makefile) */
#include <gsKit.h>

/* يرجّع نفس GSGLOBAL* اللي هيّأه المحرك مرة واحدة عند بدء تشغيل
 * اللعبة على PS2 (gsKit_init_global بكود تشغيل اللعبة، خطوة قادمة
 * منفصلة) - أي عقدة تحتاج ترسم أو تحمّل قوام تناديها */
GSGLOBAL *engine_get_gs_global(void);

#endif /* ENGINE_CONTEXT_H */
